# Local Docker secrets

Create these files locally before starting the Compose services:

```text
secrets/mysql_root_password.txt
secrets/mysql_password.txt
```

Each file should contain only the secret value. These files are ignored by Git.
